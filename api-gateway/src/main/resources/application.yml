server:
  port: 8080

spring:
  application:
    name: api-gateway

  data:
    redis:
      host: localhost
      port: 6379

  cloud:
    gateway:
      server:
        webflux:
          globalcors:
            cors-configurations:
              '[/**]':
                allowedOrigins: "*"
                allowedMethods: "*"
                allowedHeaders: "*"

          routes:
            - id: auth-route
              uri: http://localhost:8080
              predicates:
                - Path=/auth/**

            - id: catalog-service
              uri: http://localhost:8081
              predicates:
                - Path=/catalog/**
              filters:
                - name: RequestRateLimiter
                  args:
                    redis-rate-limiter.replenishRate: 10
                    redis-rate-limiter.burstCapacity: 20
                    redis-rate-limiter.requestedTokens: 1
                    key-resolver: "#{@userKeyResolver}"

            - id: subscription-service-subscriptions
              uri: http://localhost:8082
              predicates:
                - Path=/subscriptions/**
              filters:
                - name: RequestRateLimiter
                  args:
                    redis-rate-limiter.replenishRate: 10
                    redis-rate-limiter.burstCapacity: 20
                    redis-rate-limiter.requestedTokens: 1
                    key-resolver: "#{@userKeyResolver}"

            - id: subscription-service-users
              uri: http://localhost:8082
              predicates:
                - Path=/users/**
              filters:
                - name: RequestRateLimiter
                  args:
                    redis-rate-limiter.replenishRate: 10
                    redis-rate-limiter.burstCapacity: 20
                    redis-rate-limiter.requestedTokens: 1
                    key-resolver: "#{@userKeyResolver}"

jwt:
  secret: OTTPlatformSecretKeyForJWTTokenGenerationAndValidation2024
  expiration: 3600000

logging:
  level:
    com.ott.gateway: DEBUG
    org.springframework.cloud.gateway: INFO
    org.springframework.data.redis: INFO

management:
  endpoints:
    web:
      exposure:
        include: [health, info, metrics, gateway]
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true

info:
  app:
    name: API Gateway
    version: 1.0.0
    description: OTT Platform - API Gateway with JWT and Rate Limiting